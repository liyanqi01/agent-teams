Dispatch a task contract to a role for execution during orchestration.

Usage:
- `task_id` must be a task created in the current run.
- `role_id` selects the executing role. The first dispatch binds that role to the task.
- `prompt` carries the coordinator's execution instruction for this dispatch attempt.
- `assigned` and `stopped` tasks can be re-dispatched with the same bound role and instance.
- `completed`, `failed`, and `timeout` tasks cannot be re-dispatched. Create a replacement task instead.
